config files
========
config files for Linux and macOS

installation
------------

```shell
git clone git@github.com:andrzejsydor/config.git
cd config
./install.sh
```

update
------

```shell
cd config
git pull && ./install.sh
```
